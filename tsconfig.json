{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "outDir": "./dir", // Вихідні файли будуть збережені в кореневій директорії
    "rootDir": "./", // Джерельні файли також знаходяться в кореневій директорії
    "strict": true
  },
  "include": [
    "*.ts" // Включити всі .ts файли в кореневій директорії
  ],
  "exclude": [
    "node_modules", // Виняток директорії node_modules
    "*.d.ts" // Виняток типових декларацій, якщо є
  ]
}
